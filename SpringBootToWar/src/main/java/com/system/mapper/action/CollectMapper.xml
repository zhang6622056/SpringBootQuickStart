<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.system.mapper.action.CollectMapper" >
    <insert id="collectAlbum" parameterType="java.util.Map">
        INSERT INTO t_album_collect (user_id, album_id, col_time)
        VALUES
	    (#{userId},#{albumId},#{colTime})
    </insert>


    <insert id="collectStory" parameterType="java.util.Map">
        INSERT INTO t_story_collect (user_id, story_id, col_time)
        VALUES
	    (#{userId},#{albumId},#{colTime})
    </insert>

    <select id="colAlbumUser" parameterType="java.util.Map" resultType="java.lang.Long">
        SELECT
            COUNT(1)
        FROM
            t_album_collect
        WHERE
            user_id = #{userId}
        AND album_id = #{albumId}
    </select>


    <select id="colStoryUser" parameterType="java.util.Map" resultType="java.lang.Long">
        SELECT
            COUNT(1)
        FROM
            t_story_collect
        WHERE
            user_id = #{userId}
        AND story_id = #{storyId}
    </select>
</mapper>