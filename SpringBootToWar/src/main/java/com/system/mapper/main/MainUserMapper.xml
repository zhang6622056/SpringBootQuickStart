<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.system.mapper.main.MainUserMapper" >


    <resultMap id="mainUserMap" type="com.system.biz.rpc.response.main.MainUserResponse">
        <result column="user_id" property="userId" />
        <result column="nickname" property="nickname" />
        <result column="real_name" property="realname" />
        <result column="remark" property="remark" />
        <result column="head_img" property="headImg" />
    </resultMap>

    <select id="getMainUserList" resultMap="mainUserMap" parameterType="java.util.Map">
       SELECT
            tmu.user_id,
            tu.nickname,
            tu.real_name,
            tu.remark,
            tu.head_img
        FROM
            t_main_user tmu
        LEFT JOIN t_user tu ON tmu.user_id = tu.id
    </select>

















</mapper>